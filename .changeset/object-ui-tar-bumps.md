---
'@objectstack/cli': patch
'create-objectstack': patch
---

Bump pinned tooling dependencies:

- `@objectstack/cli`: `@object-ui/console` and `@object-ui/studio` from `^5.4.1` → `^5.4.2` (bundled Studio + Console assets).
- `create-objectstack`: `tar` from `^7.4.3` → `^7.5.15` (security + perf fixes when unpacking remote templates).

No API change.
